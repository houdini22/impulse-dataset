#include "../include.h"

namespace Impulse {

    namespace Dataset {

        namespace DatasetExporter {

            Abstract::Abstract(Dataset &dataset) : dataset(dataset) {

            }
        }
    }
}